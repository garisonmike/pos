<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain HTTP to a development machine, and nowhere else.

  Android 9 and later refuse cleartext traffic by default, which is the right
  default and stays in force for release builds: this file lives in the *debug*
  source set, so it is compiled into debug APKs only. A release build never sees
  it and keeps refusing plain HTTP.

  The named hosts are the two ways a till reaches a development backend:
  10.0.2.2 is the emulator's alias for the host machine's localhost, and the
  192.168.x address is this laptop on the local network, which is what a
  physical phone has to use - the emulator alias means nothing to real hardware.

  Add the address of whatever machine is serving the API, not a whole subnet.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.100.12</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
